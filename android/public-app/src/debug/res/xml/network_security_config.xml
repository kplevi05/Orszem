<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only overlay. Never shipped: this file exists solely in the debug source set, so
  the release build keeps the strict HTTPS-only policy from src/main.

  Cleartext is permitted for the development hosts only - the emulator's alias for the host
  machine and loopback - because the debug build's default API base URL is
  http://10.0.2.2:8080/. Without this, Android 9+ blocks the request and the debug app
  cannot reach a locally running backend at all.

  Loopback is also what MockWebServer binds to in instrumented tests.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
