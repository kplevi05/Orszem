# Őrszem Public app - release shrinking rules.
#
# Nothing app-specific is needed yet: the Phase 1 shell has no reflection, no
# serialization and no network layer. Rules are added alongside the features that
# require them, so this file does not accumulate speculative keep rules.
