<?xml version="1.0" encoding="utf-8"?>
<!--
  Release / Demo builds: TLS is mandatory and validated against the system
  trust store. Certificate validation is NEVER disabled and no custom or
  self-signed anchor is trusted.

  Cleartext stays allowed only for the documented local development targets
  (Android emulator host + loopback), so `./gradlew assembleDebug` against a
  local backend keeps working. `src/debug/res/xml/network_security_config.xml`
  relaxes this further for debug builds only.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator alias for the developer machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
